spring:
  main:
    allow-bean-definition-overriding: true
  config:
    import: optional:configserver:http://config-server:8888
  cloud:
    config:
      profile: db, eureka, flyway, instance
      name: config
  application:
    name: admin-service
server:
  forward-headers-strategy: framework
  servlet:
    context-path: /admin
  port: 0