spring:
  main:
    allow-bean-definition-overriding: true
  cloud:
    config:
      uri:
        - http://config-server:8888
      profile: db, eureka, instance
      name: config
  application:
    name: main-service
server:
  forward-headers-strategy: framework
  servlet:
    context-path: /main
  port: 0